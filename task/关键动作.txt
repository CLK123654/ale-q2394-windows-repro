读取生命周期合同和三份发布记录，按cluster对齐所有权、存储状态与维护窗。
拆开CRD和控制器的release边界，分别整理两个Chart的取值约束和模板。
执行Helm lint和template，保存四种配置的渲染清单与对象索引。
核对API版本、conversion服务引用和控制器读取版本，确认两个Chart能够配套交付。
根据所有权手续和存储状态安排兼容或切换阶段，并把结果落到对应维护窗。
整理交接说明，提醒值班发布人在窗口前复查状态变化。
