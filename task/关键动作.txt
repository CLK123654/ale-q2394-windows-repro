读取生命周期合同、所有权快照、存储版本快照与发布窗口，按cluster联结交接事实。
拆开CRD和控制器的release边界，为两个Chart整理各自的值约束与模板。
执行Helm lint和template，生成additive、prune、bridge与steady四份静态清单。
解析当次渲染的对象、API版本、conversion引用和控制器读取版本，形成对象清单。
依照owner_type和批准状态判断CRD的脱离接管条件，保留阻断原因。
结合对象重写数、转换失败数与storedVersions精确集合判断旧版本能否退场。
把所有权和退场结论落到已批准窗口，整理各集群的阶段安排与证据范围说明。
写明实施前重采集、条件回退、旧版本保留和下一窗口重排的处理边界。
