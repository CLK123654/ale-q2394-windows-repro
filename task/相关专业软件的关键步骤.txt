1.读取lifecycle_contract.json和两个starter Chart，核对版本范围与现有release边界。
2.整理policy-crds的additive与prune配置，保留CRD保护和conversion服务引用。
3.整理policy-controller的bridge与steady配置，渲染控制器运行对象。
4.对两个Chart执行helm lint，再用helm template生成四份候选清单。
5.解析YAML并核对版本、读取范围与服务引用，形成渲染对象表。
6.按cluster连接发布记录，生成所有权处理、版本切换和窗口计划。
