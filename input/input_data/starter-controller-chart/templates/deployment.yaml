apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-controller
  namespace: policy-system
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: policy-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: policy-controller
    spec:
      containers:
        - name: controller
          image: example.invalid/policy-controller:2.4.0
